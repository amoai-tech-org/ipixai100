# Audit — **IPI-835 · ONB2-INT-001 — Real Session, Crawl, Realtime Progress, and Approval Integration With Recovery**

**Linear:** [IPI-835](https://linear.app/amo100/issue/IPI-835/ipi-835-onb2-int-001-real-session-crawl-realtime-progress-and-approval)  
**Status:** ⚪ Todo · **PRs:** none · **Parent:** IPI-831  
**Audit date:** 2026-08-01 · Evidence: Linear + prod Realtime publication + live `/onboarding` walk + banner source

Legend: 🟢 correct / verified · 🟡 partial / risk · ⚪ missing / deferred · 🔴 wrong / blocker

---

## Plain English

This is the “make onboarding real” ticket: save drafts, create the brand, run a real crawl, show honest progress, approve DNA, recover if the tab dies. Today screen 12 is a fake timer and Brand Hub’s banner can’t hear `brands` updates because Realtime never publishes that table.

---

## Scorecard

| Dimension | Score | Dot | Note |
| --- | ---: | --- | --- |
| Spec accuracy (Realtime bugs) | **93%** | 🟢 | `brands` not in publication; `.subscribe()` no status — confirmed |
| Reuse strategy (don’t rebuild) | **90%** | 🟢 | Approve / promote / expire cron / crawl — correct |
| Dependency graph | **70%** | 🟡 | Hard-blocks 837; overstates for slice A |
| Scope size / blast radius | **55%** | 🔴 | Four slices spanning migration + cutover + delete legacy — mega-task |
| Progress UX design (4 states) | **88%** | 🟢 | Client never marks failed — excellent rule |
| Coverage of **onboarding** screen 12 | **60%** | 🟡 | Ticket centers Brand Hub banner; v2 uses `analysis-progress-screen.tsx` |
| Will succeed if executed as one lump? | **40%** | 🔴 | Too big; will mix concerns / regress |
| Will succeed if sliced + deps Done? | **72%** | 🟡 | Achievable with discipline |
| Overall task health | **68%** | 🟡 | Right destination; dangerous packaging |

**Not 100% correct** — dependency hardness, delete-legacy timing, onboarding vs Brand Hub progress UI gap, overlap with canceled IPI-808.

---

## What Linear says vs reality

| Claim | Reality | Dot |
| --- | --- | --- |
| `brands` not in `supabase_realtime` | ✅ Prod publication = `brand_crawls` + `brand_crawl_results` only | 🟢 |
| Banner `.subscribe()` has no status callback | ✅ `analysis-progress-banner.tsx` | 🟢 |
| No client stall detector | ✅ | 🟢 |
| `expire_stale_brand_analysis` exists + cron | ✅ Migrations + types | 🟢 |
| Column-list publication needs PK `id` | ✅ Postgres 15+; good warning | 🟢 |
| No client `select:` filter for columns | ✅ Correct Supabase API note | 🟢 |
| Blocked by 832 + 833 + 834 | ✅ 833 Done; 832/834 Todo — hard for full wiring | 🟢 |
| Blocked by 837 | 🟡 Prefer for Google entry; **not** required for publication slice A | 🟡 |
| Delete `/app/onboarding` in this task | 🔴 High risk before Playwright (**IPI-836**) proves v2 | 🔴 |
| Screen 12 fake timer | ✅ Live walk: `#12` auto-advances; component is timer-based | 🟢 |
| Approval → `ready` via `promoteBrandDraft` | ✅ Spec correctly rejects old `scores_complete` myths | 🟢 |
| Related IPI-808 | 🔴 **Canceled** — content largely superseded by 835/836; don’t revive | 🔴 |

---

## Errors · red flags · failure points

| # | Severity | Issue | Fix |
| --- | --- | --- | --- |
| 1 | 🔴 | **Scope bomb:** session wire + Realtime + approval + **delete legacy** in one issue | Keep 4 PR slices; **do not delete** `/app/onboarding` until **IPI-836** green (redirect/flag instead) |
| 2 | 🔴 | **Hard `blockedBy` IPI-837** blocks starting publication | Soften: 837 preferred; slice A (publication) unblocked now |
| 3 | 🔴 | **Wrong progress surface risk:** ticket fixes Brand Hub banner; v2 onboarding uses `AnalysisProgressScreen` (fake timer) | Explicit AC: replace/wire **onboarding screen 12** to real crawl truth (reuse formatter/banner patterns) |
| 4 | 🔴 | **Starting before 832+834** | Do not wire materialize/DNA review until those land; only slice A can precede |
| 5 | 🟡 | **Worker bundle / OpenNext size** (related IPI-706) | Keep session client thin; no new heavy deps; watch `build:cf` |
| 6 | 🟡 | **Approval UI location unspecified** | Spell: screen 13 becomes evidence review (IPI-834 shape) + existing approve API — not a new route |
| 7 | 🟡 | **Hash / resume vs address bar** (IPI-840) | On resume, `replaceState` to `#${current_screen}` from session |
| 8 | 🟡 | **QA DB** needed for publication pgTAP + orchestration | Track **IPI-829**; don’t run publication experiments only on prod without rollback plan |
| 9 | 🟡 | **Double Realtime subscriptions** if onboarding + banner both listen naively | One shared hook/store for brand progress |
| 10 | ⚪ | **Idempotent crawl kickoff** under resume | AC: re-entry must not start a second crawl if one is in flight (tie to analysis lock) |
| 11 | ⚪ | **Feature flag / allowlist** for prod cutover | Prefer server allowlist (IPI-836) over `NEXT_PUBLIC_ONBOARDING_V2_ENABLED` (stale IPI-808 pattern) |

---

## What’s missing from the ticket

- AC mapping **onboarding screen 12/13** ↔ Realtime / DNA review (not only Brand Hub banner)
- Explicit **do not delete legacy until 836** (or delete behind allowlist with instant rollback)
- **Crawl idempotency** on resume / double Continue
- Soften Linear **blockedBy IPI-837**
- Link **IPI-829** as related for pgTAP `009`
- Cancel/supersede note for **IPI-808** so agents don’t implement both
- Bundle budget check in AC (not only lint/typecheck)

---

## Critical fixes (before implementation)

1. **Unblock slice A now:** migration `alter publication … add table brands (id, intake_status, updated_at)` + pgTAP `009` — own PR.  
2. **Edit Linear deps:** 837 = preferred, not hard block for A.  
3. **Rewrite Done criteria** to name `analysis-progress-screen.tsx` / flow wiring.  
4. **Move “delete `/app/onboarding`”** to final slice after 836 happy path — or replace with redirect.  
5. **Wait for 832 + 834** before slices B/D (session + approval).

---

## Best path to complete (efficient)

```text
Unblocked now:
  835-A  Realtime publication + pgTAP 009          (∥ 837, ∥ 832A, ∥ 834)

After 832 + 834 (+ 837 preferred):
  835-B  Session create/resume/autosave on /onboarding
  835-C  Honest progress (screen 12 + banner status callback)
  835-D  Materialize → crawl → approve → ready; legacy retire LAST

Then:
  IPI-836 Playwright + allowlisted prod smoke
```

| Approach | Verdict |
| --- | --- |
| Four slices as above | 🟢 Best |
| One PR “wire everything + delete old” | 🔴 Will fail one-concern + rollback |
| Custom poller instead of Realtime | 🔴 Violates native-first (IPI-881) |
| Client timer that marks failed | 🔴 Explicitly forbidden — keep server cron |
| Rebuild approval endpoint | 🔴 Out of scope |

**Ponytail:** highest leverage first line of SQL is the publication. Fake progress is the user-visible lie; fix publication + subscribe status before polishing copy.

---

## Dependency truth (corrected)

```text
IPI-833 🟢 Done ─────────────────────────────┐
IPI-837 🟡 preferred (Google entry) ─────────┤
IPI-832 ⚪ required for B/D ─────────────────┼──► IPI-835 (this)
IPI-834 ⚪ required for DNA review / D ──────┤
IPI-829 ⚪ required for honest DB proof ─────┘
         │
         ▼
      IPI-836
```

---

## Success odds

| Path | Odds |
| --- | --- |
| Sliced A→B→C→D after 832/834; legacy delete after 836 | 🟢 **~70%** |
| Start full wiring tomorrow with no 832/834 | 🔴 **~25%** |
| Delete `/app/onboarding` in first code PR | 🔴 **~30%** — prod escape hatch gone |
| Fix banner only, leave onboarding timer | 🟡 **~45%** — Brand Hub better; wizard still lies |

---

## Bottom line

🟢 **Problem diagnosis is excellent** (Realtime + silent subscribe + fake timer).  
🟡 **Ticket is ~68% healthy** — right goal, oversized packaging.  
🔴 **Not 100%:** hard OAuth block, legacy delete timing, onboarding screen-12 AC gap, canceled-808 overlap.  
**Efficient play:** land **835-A publication now**; finish **832/834/837**; then wire; prove with **836** before killing the old route.
