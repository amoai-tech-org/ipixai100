# Audit — **IPI-837 · AUTH-OAUTH-001 — Preserve Safe Post-Login Redirect Through Google OAuth**

**Linear:** [IPI-837](https://linear.app/amo100/issue/IPI-837/auth-oauth-001-preserve-safe-post-login-redirect-through-google-oauth)  
**Status:** 🟡 In Progress · **PR:** [#700](https://github.com/amo-tech-ai/lumina-studio/pull/700) MERGEABLE, CI green  
**Audit date:** 2026-08-01 · Evidence vs `origin/main` + PR #700 + prod auth gate

Legend: 🟢 correct / verified · 🟡 partial / risk · ⚪ missing / deferred · 🔴 wrong / blocker

---

## Plain English

Google sign-in currently throws people into `/app` even when they came from `/onboarding`. Email already keeps the redirect. This task carries a safe destination through OAuth (cookie) and lands them back on the wizard.

---

## Scorecard

| Dimension | Score | Dot | Note |
| --- | ---: | --- | --- |
| Spec accuracy (defect description) | **95%** | 🟢 | Matches `main` callback hardcode + login Google gap |
| Scope clarity | **90%** | 🟢 | Tight; out-of-scope clear |
| Dependencies / sequencing | **70%** | 🟡 | Overstates 882/877 as launch steps 1–2 |
| Acceptance criteria quality | **88%** | 🟢 | Smoke required — right gate |
| Implementation readiness (PR) | **92%** | 🟢 | Code + Stage 0 + units done |
| Production completeness | **55%** | 🟡 | Smoke unchecked; not on `main` |
| Security / redirect hygiene | **90%** | 🟢 | Reuses `safeRedirect()`; Option B justified |
| Will succeed if executed as written? | **85%** | 🟢 | High — only smoke + merge left |
| Overall task health | **82%** | 🟢 | Best-shaped ticket of the three |

**Not 100% correct** — sequencing fluff + title + unchecked smoke. Core technical claim is solid.

---

## What Linear says vs reality

| Claim | Reality | Dot |
| --- | --- | --- |
| Email keeps `redirect`; Google drops it | ✅ Confirmed on `main` (`successUrl = /app`; Google `redirectTo` has no next) | 🟢 |
| `safeRedirect` already allows `/onboarding` | ✅ On `main` after IPI-833 | 🟢 |
| Prefer cookie (Option B) for exact callback allowlist | ✅ PR Stage 0 lists exact `/auth/callback` **and** `/**` wildcards; Option B still correct (don’t depend on wildcard) | 🟢 |
| Blocks IPI-835 | 🟡 Prefer for Google entry; not a hard code block for Realtime/migration slices | 🟡 |
| Launch steps 1–2 = IPI-882 / IPI-877 | 🔴 Payments + native-first are **not** blockers for OAuth | 🔴 |
| Stage 0 Redirect URLs required | ✅ Done in PR #700 body | 🟢 |
| Smoke on preview/prod | 🔴 Still unchecked on PR checklist | 🔴 |
| Title format `IPI-837 · …` | 🟡 Linear title is `AUTH-OAUTH-001 — …` (missing ID prefix) | 🟡 |

---

## Errors · red flags · failure points

| # | Severity | Issue | Fix |
| --- | --- | --- | --- |
| 1 | 🔴 | **Smoke not done** — only remaining real AC; CodeRabbit already flagged | Run preview: signed-out `/onboarding` → Google → land `/onboarding`; unsafe target → `/app`; then merge |
| 2 | 🔴 | **Launch table lists 882/877 as prerequisites** | Edit Linear: OAuth is next after **IPI-833 Done**; 877/882 are parallel governance, not blockers |
| 3 | 🟡 | **Hash `#1` cannot survive OAuth** (fragment never sent to server/cookie path) | Document as out of scope / **IPI-840**; post-login default screen 1 is fine |
| 4 | 🟡 | Linear title missing `IPI-837 ·` | Rename to full task-naming form |
| 5 | 🟡 | SLA high-risk window open (breaches ~2026-08-04) | Finish smoke this weekend; don’t let ticket rot In Progress |
| 6 | ⚪ | Preview allowlist for PR preview host | Confirm Stage 0 wildcards cover `ipix-*-operator-mdeai.vercel.app` (PR claims yes) |

---

## What’s missing

- [ ] Checked-off Google smoke evidence (URL + outcome) in Linear comment / PR
- [ ] Explicit note: email path already OK — don’t regress in review
- [ ] Post-merge: mark Linear Done only after prod smoke (or preview + short prod allowlisted)

---

## Critical fixes (do these)

1. **Finish the two smoke journeys on the PR preview** (15 min).  
2. **Merge #700** — unblocks Google → wizard.  
3. **Edit Linear launch sequence** so 832/834 are not waiting on 882/877.

---

## Best path to complete (efficient)

| Step | Action | Timebox |
| --- | --- | --- |
| 1 | Preview Google smoke + unsafe fallback | 15–30 min |
| 2 | Merge PR #700 (one concern already clean) | — |
| 3 | Optional prod smoke with allowlisted account | 10 min |
| 4 | Linear → Done | — |

**Do not:** reopen Stage 0, rewrite carrier as query string, touch onboarding screens, or mix docs into the PR.

**Better than the ticket?** No — Option B + `safeRedirect` is the right minimal design. Don’t invent a second redirect validator.

---

## Success odds

| If… | Odds |
| --- | --- |
| Smoke green → merge | 🟢 **~90%** ship this week |
| Skip smoke, merge on units only | 🟡 **~60%** — silent prod miss possible |
| Wait for 882/877 | 🔴 Wrong — delays for no reason |

---

## Bottom line

🟢 **Spec + PR are ~ready.** The defect is real; the fix is small and already coded.  
🔴 **Only blocker to Done:** unchecked Google smoke.  
🟡 **Not 100% correct:** launch ordering noise and hash limitation undocumented.
