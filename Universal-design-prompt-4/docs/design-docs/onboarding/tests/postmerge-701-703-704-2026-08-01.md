# Test Report — Post-merge #701 / #703 / #704

**Date:** 2026-08-01  
**Baseline:** `origin/main` @ `e0706b13` (docs #705 after code merges)  
**Squash commits:**

| PR | Task | SHA |
| --- | --- | --- |
| [#701](https://github.com/amo-tech-ai/lumina-studio/pull/701) | **IPI-832 · ONB2-DB-001** slice A (DB) | `91bf039505f5616e5ce8c560327c041a28ece940` |
| [#703](https://github.com/amo-tech-ai/lumina-studio/pull/703) | **IPI-832 · ONB2-DB-001** slice B (app→RPC) | `f06c79172210ca438948429fc5c8551010afdd1b` |
| [#704](https://github.com/amo-tech-ai/lumina-studio/pull/704) | **IPI-834 · ONB2-AI-001** Brand DNA | `ce1f8d37f25d10c60bad2558cf672dd39cbb672e` |

**Projects:** prod `nvdlhrodvevgwdsneplk` · QA `wtuhdynujhszsbwxlbdi`  
**Constraint:** No destructive writes to production. QA used for pgTAP.  
**Canvas:** `~/.cursor/projects/home-sk-ipix/canvases/onboarding-postmerge-audit-701-703-704.canvas.tsx`

Legend: ✅ PASS · ⚠ PASS with gap · ❌ FAIL · ⚪ NOT RUN / schema-only

---

## Verdict

| Gate | Result |
| --- | --- |
| #701 database foundation | ✅ PASS |
| #703 app integration | ⚠ PASS (resume gap) |
| #704 Brand DNA contract | ✅ PASS |
| Production schema verified | ✅ YES |
| QA concurrency verified | ❌ NO |
| Tenant isolation verified | ⚪ SCHEMA ONLY |
| Fail-closed AI validation | ✅ YES (unit + Deno) |
| **Overall confidence** | **78%** |
| **Verdict** | **PASS WITH FOLLOW-UPS** |

**Top failure point:** `/app/onboarding` mints `crypto.randomUUID()` per mount — refresh loses the idempotency key (resume deferred to **IPI-835**).  
**Next safest action:** merge [#702](https://github.com/amo-tech-ai/lumina-studio/pull/702) (**IPI-891**), then run **IPI-894** race on QA.

---

## 1. Merge verification

| Check | Result | Evidence |
| --- | --- | --- |
| All three SHAs on `origin/main` | ✅ | `git merge-base --is-ancestor` for each |
| No later revert of A/B/DNA paths | ✅ | Only #705 docs after code; no churn under `onboarding/` / `20260801051934` / brand-profile |
| Prod ↔ QA RPC body identical | ✅ | `md5(pg_get_functiondef)` = `4df92f1212c30814af232d6d3ec743a1` both sides |

---

## 2. Area results

| Area | Result | Evidence | Failure risk | Fix |
| --- | --- | --- | --- | --- |
| #701 DB foundation | ✅ | Table + RLS + mig `20260801051934` on prod+QA; INVOKER; `search_path=public`; anon/PUBLIC EXECUTE false; authenticated EXECUTE true | Low | **IPI-893**, **IPI-894** |
| #703 App → RPC | ⚠ | `createOrgAndBrand` → `materialize_onboarding_session`; key rotate on name/URL; `buildShellAiProfile` write; Vitest green | Med — no cross-visit resume | **IPI-835** |
| #704 Brand DNA | ✅ | JSON Schema SSOT; Edge+Mastra `validateBrandProfilePayload`; Deno 5/5; parity 5/5; brand-hub unwrap | Low | Optional Playwright |
| CI soft fails | ⚠ known | Remote-only `20260801051614` → **IPI-891** / #702; booking-gate IPv6 → **IPI-892** / #706 | CI noise | Merge #702; finish #706 |

---

## 3. Tests run (exact)

### 3a. Vitest (detached `origin/main` worktree)

```bash
cd app
npx vitest run \
  src/test/onboarding.test.ts \
  src/test/onboarding-orchestration.test.ts \
  src/lib/brand/brand-profile-contract.parity.test.ts \
  src/mastra/workflows/brand-intelligence-workflow.test.ts
```

| File | Tests | Result |
| --- | ---: | --- |
| `onboarding.test.ts` | 19 | ✅ |
| `onboarding-orchestration.test.ts` | 4 | ✅ |
| `brand-profile-contract.parity.test.ts` | 5 | ✅ |
| `brand-intelligence-workflow.test.ts` | 26 | ✅ |
| **Total** | **54** | ✅ |

Also: `src/test/brand-hub.test.ts` — **26/26** ✅ (parseAiProfile claim unwrap).

### 3b. pgTAP on QA

```bash
supabase test db --db-url "$QA_DATABASE_URL" \
  supabase/tests/database/008_onboarding_sessions.sql
```

| Suite | Assertions | Result |
| --- | ---: | --- |
| `008_onboarding_sessions.sql` | 17 | ✅ **PASS** (`All tests successful`) |

Note: suite mirrors DDL inside `begin…rollback` (same pattern as `007`) — proves contract, not a second apply of the live migration.

### 3c. Deno (Edge Brand DNA)

```bash
cd supabase/functions
deno test --allow-read --allow-env _shared/schemas/brand-profile.test.ts
```

| Suite | Tests | Result |
| --- | ---: | --- |
| `brand-profile.test.ts` | 5 | ✅ (needs `--allow-env`) |

### 3d. Not claimed green this pass

| Gate | Status | Reason |
| --- | --- | --- |
| `npm run typecheck` / full `npm test` / `npm run build` | ⚪ | Detached tree + symlinked `node_modules` missing some packages; failures not attributed to these PRs |
| Playwright E2E onboarding | ⚪ | Not run |
| Live `Promise.all` race | ❌ | Owned by **IPI-894** |
| Dual-JWT tenant probe | ⚪ | Schema policies only |

---

## 4. Live Supabase verification

| Check | Prod `nvdlh…` | QA `wtuhdy…` |
| --- | --- | --- |
| `onboarding_sessions` table | exists | exists |
| RLS enabled | true | true |
| Session rows (at audit) | 0 | 0 |
| mig `20260801051934` | recorded | recorded |
| mig `20260801051614` | recorded (**remote-only vs main**) | recorded |
| RPC count | 1 | 1 |
| `SECURITY DEFINER` | false (INVOKER) | false |
| `proconfig` | `search_path=public` | same |
| EXECUTE anon / PUBLIC | false / false | false / false |
| EXECUTE authenticated | true | true |
| status CHECK | `draft \| materialized` | same |
| unique `(user_id, idempotency_key)` | `onboarding_sessions_user_key` | same |
| FKs `ON DELETE SET NULL` | org + brand | same |
| RPC body MD5 | `4df92f12…` | **identical** |

**Policies (prod, verified):**

| Policy | Command | Shape |
| --- | --- | --- |
| `onboarding_sessions_select_own` | SELECT | `auth.uid() = user_id` |
| `onboarding_sessions_insert_own` | INSERT | own + `status=draft` + null outcomes |
| `onboarding_sessions_update_own` | UPDATE | draft **or** `app.onboarding_materializing=on` |
| `onboarding_sessions_delete_own` | DELETE | `auth.uid() = user_id` |

---

## 5. Real-world scenario matrix

| # | Scenario | Result | Note |
| --- | --- | --- | --- |
| 1 | Normal create → 1 session/org/brand | ✅ unit | Mocked RPC; no Playwright |
| 2 | Double-click Continue | ✅ unit | Same key + UI `loading` / shell reuse |
| 3 | Concurrent same key | ❌ not run | **IPI-894** |
| 4 | Refresh resume draft | ⚠ **gap** | Key = `useState(UUID)`; localStorage = **IPI-835** |
| 5 | Edit name/URL before submit | ✅ code | `setField` rotates key + clears shell |
| 6 | RPC fails after org insert | ✅ pgTAP | Forced brand-insert trigger → atomic rollback |
| 7 | Retry after temporary failure | ✅ design | Replay same IDs; shell write retriable |
| 8 | User A vs User B | ⚪ schema | Own-row policies; no dual-JWT runtime |
| 9 | Valid evidence Brand DNA | ✅ unit+Deno | Accept path |
| 10 | Empty quote / bad URL / no evidence | ✅ unit+Deno | Fail closed |
| 11 | Delete brand/org after materialize | ⚪ schema | `ON DELETE SET NULL`; no live delete |

---

## 6. Confirmed errors / red flags

| Severity | Finding | Classification |
| --- | --- | --- |
| 🟡 | Refresh abandons draft identity (new UUID each mount) | Confirmed code gap → **IPI-835** |
| 🟡 | Key rotate leaves abandoned draft rows (no partial unique on `status=draft`) | Confirmed design/product choice |
| 🟡 | `brands.ai_profile` shell write can throw **after** successful materialize | Recoverable via replay; thin `shellErr` test |
| 🟡 | Race concurrency unproven live | Confirmed → **IPI-894** |
| 🟡 | Direct UPDATE bypass / cross-user key not in pgTAP 008 | Confirmed → **IPI-893** |
| ⚠ infra | `20260801051614` on remotes, missing from `main` ledger | **IPI-891** / #702 — not an onboarding regression |
| ⚠ infra | CI `booking-gate` IPv6 | **IPI-892** / #706 |

No blocking defect found in the merged #701/#703/#704 code paths themselves.

---

## 7. Missing tests / AC

| Gap | Tracker |
| --- | --- |
| INSERT/DELETE policy + UPDATE-bypass + P0002 + cross-user key in `008` | **IPI-893 · ONB2-DB-001b** |
| Live `Promise.all` identical org/brand | **IPI-894 · ONB2-DB-001c** |
| localStorage resume / crawl UI | **IPI-835 · ONB2-INT-001** |
| Playwright happy-path + double-submit | **IPI-836** (final verify) when ready |

---

## 8. Follow-up task order (safest)

1. Merge [#702](https://github.com/amo-tech-ai/lumina-studio/pull/702) — **IPI-891 · SB-DRIFT-001**  
2. Finish **IPI-892** / [#706](https://github.com/amo-tech-ai/lumina-studio/pull/706) — booking-gate IPv4/pooler  
3. **IPI-894** — QA race  
4. **IPI-893** — harden `008`  
5. **IPI-835** — session resume + crawl (closes refresh gap)

---

## 9. Scorecard (copy-paste)

```text
#701 database foundation: PASS
#703 app integration: PASS (resume gap)
#704 Brand DNA contract: PASS
Production schema verified: YES
QA concurrency verified: NO
Tenant isolation verified: SCHEMA ONLY
Fail-closed AI validation verified: YES (unit + Deno)
Overall post-merge confidence: 78%
Verdict: PASS WITH FOLLOW-UPS

Top error: none blocking in merged code
Top failure point: refresh loses idempotency key
Next safest action: merge #702 (IPI-891), then run IPI-894 on QA
```
