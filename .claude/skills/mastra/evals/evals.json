{
  "skill_name": "mastra",
  "evals": [
    {
      "id": 1,
      "prompt": "We're on Node 22 in `/home/sk/ipixai`. I upgraded `@mastra/core` and now TypeScript says `Property 'memory' does not exist on type 'AgentConfig'`. I don't want guesses \u2014 what\u2019s the current API and where do I verify it in our install?",
      "expected_output": "Agent directs to embedded docs under `node_modules/@mastra/*/dist/docs/`, SOURCE_MAP or grep; mentions `references/common-errors.md` and version mismatch; does not invent a new AgentConfig shape from memory.",
      "files": [],
      "expectations": [
        "Response tells the user to verify against installed package docs or types, not training data alone",
        "Mentions embedded docs path pattern (`node_modules/@mastra/.../dist/docs`) or `references/embedded-docs.md`",
        "References `references/common-errors.md` or equivalent troubleshooting path"
      ]
    },
    {
      "id": 2,
      "prompt": "I need the official Mastra doc URL for human-in-the-loop workflows and the local skill file that maps 'intent \u2192 links.md section' so I can bookmark both.",
      "expected_output": "Correct `links.md` anchor or URL from the Workflows / HITL area; points to `references/topic-routing.md` (or `links.md` directly) for intent routing.",
      "files": [],
      "expectations": [
        "Includes a mastra.ai docs URL for human-in-the-loop or workflows (or defers to `links.md` as canonical index)",
        "Names `references/topic-routing.md` or `links.md` as the local routing artifact"
      ]
    },
    {
      "id": 3,
      "prompt": "Add a new agent in our Mastra app using `google/gemini-3-pro-preview`. Boss insists the string is valid today. What do I run before hardcoding the model id?",
      "expected_output": "Run `node scripts/provider-registry.mjs` (with provider flag as needed); avoid asserting model availability from memory.",
      "files": [],
      "expectations": [
        "Instructs to run bundled `scripts/provider-registry.mjs` or equivalent registry lookup",
        "Uses `provider/model` Mastra convention in explanation"
      ]
    },
    {
      "id": 4,
      "prompt": "Codex agent has no `node_modules` in this sandbox \u2014 how should I pull current Mastra docs text for context, and what\u2019s the llms-friendly URL pattern?",
      "expected_output": "Remote path: `references/remote-docs.md`, `https://mastra.ai/llms.txt`, append `.md` to doc URLs when fetching.",
      "files": [],
      "expectations": [
        "Mentions `mastra.ai/llms.txt` and/or `references/remote-docs.md`",
        "Mentions appending `.md` to doc URLs for markdown fetch"
      ]
    },
    {
      "id": 5,
      "prompt": "Wire `MCPClient` so my Mastra agent can call the Mastra docs MCP server. I\u2019m lost between `@mastra/mcp` and the npx server package \u2014 what does the skill say?",
      "expected_output": "Points to SKILL.md MCP section + official MCP overview URL; shows or cites MCPClient + `npx -y @mastra/mcp-docs-server@latest` pattern.",
      "files": [],
      "expectations": [
        "References `@mastra/mcp` / `MCPClient` and the docs server package name",
        "Links or names `https://mastra.ai/docs/mcp/overview` (or `links.md` MCP section)"
      ]
    },
    {
      "id": 6,
      "prompt": "Studio isn\u2019t up. I need to list agents on the local Mastra server and then fetch traces. What do I start in this iPixai repo, and which mastra api commands?",
      "expected_output": "Start npm run dev:agent on :4111 (not combined npm run dev). Check the local CLI with npx --no-install mastra --version and npx --no-install mastra api --help. If api exists: npx --no-install mastra api --url http://localhost:4111 agent list and a local trace list/get. If api is missing, fall back to swagger-ui or curl /api/system/api-schema. Points at references/mastra-api.md.",
      "files": [],
      "expectations": [
        "Names `npm run dev:agent` on :4111 and forbids combined `npm run dev`",
        "Checks the local CLI with `npx --no-install mastra --version` and `npx --no-install mastra api --help` before assuming `mastra api` exists",
        "If `mastra api` is available: uses `npx --no-install mastra api --url http://localhost:4111 agent list`",
        "If `mastra api` is available: uses a local trace command such as `npx --no-install mastra api --url http://localhost:4111 trace list` or `trace get`"
      ]
    },
    {
      "id": 7,
      "prompt": "For the iPix Production Planner, should reusable fashion-production instructions live in a workspace skill, an agent-level skill, or hard security code? Verify the current installed Mastra capability first.",
      "expected_output": "Uses installed docs/types first; explains Agent({ skills }) / createSkill for self-contained guidance, workspace skills for filesystem/shared discovery, and keeps auth/RLS/HITL/deterministic rules in code.",
      "files": [],
      "expectations": [
        "Mentions agent-level skills or createSkill",
        "Distinguishes workspace skills from agent-level skills",
        "Does not move authorization or approval gates into a skill"
      ]
    },
    {
      "id": 8,
      "prompt": "Our future Mastra agent has 80 tools and 30 skills. Should we load everything every turn? What current Mastra processors should we evaluate, and should Planner Core use them now?",
      "expected_output": "Mentions SkillSearchProcessor and ToolSearchProcessor, verifies installed API first, and says they are optimizations for measured large-catalog problems rather than Core Planner dependencies.",
      "files": [],
      "expectations": [
        "Mentions SkillSearchProcessor",
        "Mentions ToolSearchProcessor",
        "States Core Planner with four tools should not adopt them without need"
      ]
    },
    {
      "id": 9,
      "prompt": "We need to understand recurring Planner failures across many production traces, not debug one request. Which Mastra capability/reference should we check?",
      "expected_output": "Routes to Trace Intelligence for aggregate themes when available, notes platform/private-beta constraints, then drills into traces/logs/scores for evidence.",
      "files": [],
      "expectations": [
        "Mentions Trace Intelligence",
        "Notes aggregate themes/agent health",
        "Does not treat it as iPix business truth"
      ]
    }
  ]
}
